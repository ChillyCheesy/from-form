import { InjectionToken } from '@angular/core';
import { FrmContextProvider } from '../public-api';

export const FROM_FORM_CONTEXT_PROVIDER: InjectionToken<FrmContextProvider> = new InjectionToken<FrmContextProvider>('FROM_FORM_CONTEXT_PROVIDER');
